apiVersion: v1
kind: Service
metadata:
  name: openai-chatbot-service
spec:
  selector:
    app: openai-chatbots
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
